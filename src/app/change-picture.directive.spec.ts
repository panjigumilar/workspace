import { ChangePictureDirective } from './change-picture.directive';

describe('ChangePictureDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangePictureDirective();
    expect(directive).toBeTruthy();
  });
});
